# 稿件修订报告

## 本轮已完成

1. 将题名、摘要、引言和结论改为“离线模型/算法性能分析”口径，删除未经证据支持的高保真、在线实时、完全消除和物理闭环表述。
2. 按 `stage14_comparison.m` 的实际 14 个 `build_cases` 重写工况表，移除该批次未执行的 40 dB 和三次谐波工况。
3. 将四相误差模型改写为源码一致的幅值、相位、偏置、高斯噪声和可选公共干扰形式，并注明 Stage 14 中高频干扰幅值为零。
4. 补充扭矩—扭转角—位移—相位的总传递式、逆变换和一阶误差传播式。
5. 将“Existing improved”改写为源码实际实现的中位数去偏置、RMS归一化、协方差白化、反正切和残余相位校正流程。
6. 将专利基准解码改写为 ZERO 参考、互补差分、unwrap 和有符号完成区间计数流程。
7. 明确 Stage 14 算法比较直接接收模拟模拟通道，不经过 ADC；ADC/串口模块仅作为独立接口。
8. 明确温度系数为 `simulation/default`、未验证；补偿后接近零是同一模型正逆运算的一致性结果。
9. 新增 `PAPER_AUDIT.md`，记录问题等级、数据边界、可保留数值和验收标准。

## 图件重构（2026-09-22）

- 用 `figures_9_20260922/fig01` 替换正文架构 TikZ 图，并新增结构图、信号链图、算法流程图和协议接口图（fig02--fig05）。
- 用 `fig06_rmse_heatmap_python.png` 和 `fig07_method_summary_python.png` 替换原 Stage 14 PDF 图件；二者继续使用既有 Stage 14 CSV 数据。
- 用 `fig08_temperature_compensation_python.png` 替换温度结果图，并加入 `fig09_software_gui_real_runtime.png` 作为软件运行态界面示例。
- 正文已在每个图首次出现处使用 `\\ref` 引用；图注说明示意图、协议预览和软件界面不等同于实物测量证据。
- 新版 XeLaTeX PDF 为 14 页，图文件均来自 `figures_9_20260922`，无旧 Stage 14/15 PDF 图引用。
- 图9已替换为用户提供的 `fig09_software_gui_user_runtime.png`；新版PDF第12页已完成图9视觉检查。

## 未擅自修改

- 未修改冻结的旧版目录 `paper_grating_torque_twin`。
- 未改动原始 MATLAB 数据、已有 CSV/PDF 图源和参考文献条目。
- 未声称已完成物理样机标定、真实串口通信、FPGA 部署或在线硬件在环。

## 待投稿前人工核验

- 作者、单位、基金、收稿日期和通信邮箱仍是模板信息。
- 参考文献需按目标期刊要求逐条核验作者、卷期、页码和 DOI。
- 需要用实物台架重新标定 $G(T)$、$P(T)$、偏置和通道幅相参数，并补充独立实验数据。
